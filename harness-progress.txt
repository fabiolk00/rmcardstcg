[QA] 2026-06-17 cupom.code.ci-unique -> APROVADO (passes=true, qa_note=null, state inalterado=ready_for_qa).
  Repro: pnpm harness tests/harness/pedido/cupom-code-ci-unique.spec.ts em PG efemero recem-seedado (28 prod/8 ped) -> 1 passed (4.4s).
  Anti-fake-green: 4/4 asserts provados ponta a ponta via pg sobre DATABASE_URL; seam=createCoupon de PRODUCAO (lib/data/coupons.ts:150) via _run-seam.ts (sem mock).
    a1 {ok:false}+msg duplicado: L158-166 (P2002 real, log PG: duplicate key coupons_code_key Key (lower(code))=...).
    a2 1 linha p/ LOWER(code): L108-115 guarda indice UNIQUE LOWER(code) + L170-185 COUNT==1 e id sobrevivente=1a (anti-trivial caixa mista vs minuscula).
    a3 code em UPPER: L140-147 le code cru e == toUpperCase, != lowercase.
    a4 audit nao cresce na falha: L118-210 delta global +1 e exatamente 1 coupon.create p/ esse LOWER(code) (audit-same-tx, rollback sem orfao; action lida dotted "coupon.create").
  Invariantes coupon-code-ci-unique + audit-same-tx: ambas exercitadas no DB pela spec.
  Produto intocado: git diff e0e83a1..HEAD -- app lib prisma VAZIO.

[QA] 2026-06-17 cupom.update.audited -> APROVADO (passes=true, qa_note=null, state inalterado=ready_for_qa).
  Repro: pnpm harness tests/harness/pedido/cupom-update-audited.spec.ts em PG efemero recem-seedado (28 prod/8 ped) -> 1 passed (2.7s).
  Anti-fake-green: 3/3 asserts provados ponta a ponta via pg sobre DATABASE_URL; seam=updateCoupon de PRODUCAO (lib/data/coupons.ts:179) via _run-seam.ts (sem mock); createCoupon (L150) p/ semear o cupom inicial.
    A1 editaveis atualizados + redeemed_count==R: L171-175 semeia redeemed_count=R=7 (anti-trivial, R>0); L202-205 NEW!=INITIAL; L237-242 pos-update min=8000/max=25/redeemed=7 via pg. toCouponData NUNCA emite redeemedCount (CouponInput nao tem o campo).
    A2 coerencia tipo<->campo (CHECK valido): L143-146 sanity pg_constraint coupons_type_value_chk existe; L249-257 percent_off NOT NULL 1..100 + value_cents NULL + COUNT(*)==0 violacoes do predicado IDENTICO ao CHECK (test-schema-supplement.sql L38-41).
    A3 audit 1 linha coupon.update na mesma tx, before/after: L261-267 delta GLOBAL +1; L271-280 exatamente 1 linha action='coupon.update' (dotted cru) p/ entity_id; L285-292 before=INITIAL / after=NEW + coerencia no snapshot (type=percent, percentOff set, valueCents null). Fonte: writeAuditLog grava via tx (audit-same-tx).
  Invariantes coupon-coherence(A2)/coupon-redeem-limits(A1)/audit-same-tx(A3)/cents-only(Number.isInteger): todas exercitadas no DB pela spec.
  Produto intocado: git diff e0e83a1..HEAD -- app lib prisma VAZIO. Seam dispara updateCoupon/createCoupon de PRODUCAO (sem mock).

[QA] 2026-06-17 cupom.redeem.idempotent-per-order -> APROVADO (passes=true, qa_note=null, state inalterado=ready_for_qa).
  Repro: pnpm harness tests/harness/pedido/cupom-redeem-idempotent-per-order.spec.ts em PG efemero recem-seedado (28 prod/8 ped) -> 1 passed (5.3s).
  Anti-fake-green: 3/3 asserts provados ponta a ponta via pg sobre DATABASE_URL; seam=redeemCoupon de PRODUCAO (lib/data/coupons.ts:364) via _run-seam.ts case redeemCoupon (L465-482, envelopa em $transaction como o checkout; RedeemAbort so em ok:false); createCoupon (L150) p/ semear o cupom. Sem mock.
    Sanity: L132-139 indice UNIQUE coupon_redemptions.order_id (fonte da idempotencia) existe; L128-131 CHECK coupons_redeemed_count_chk existe. Schema: orderId @unique @map order_id; userId @map clerk_user_id (spec le clerk_user_id AS user_id, ok).
    A1 ok:true alreadyRedeemed=true no-op: L184-194 1a redencao do orderX -> r1.ok=true/alreadyRedeemed=false; L209-224 2a do MESMO orderX -> r2.ok=true/alreadyRedeemed=true. Valores do retorno da funcao de PRODUCAO (curto-circuita em findUnique(orderId), L375-376).
    A2 1 unica linha p/ orderId X: L197-201 COUNT==1 apos 1a; L227-237 rowCount==1 apos 2a E a unica linha e do userId da 1a (a 2a nao inseriu). Via pg.
    A3 redeemed_count NAO incrementa na repeticao: L202-206 ==1 apos 1a; L240-248 continua ==1 apos 2a + Number.isInteger; L252-260 rede final 0 cupons violam redeemed_count>=0 AND <=max. Anti-trivial: pre redeemed_count=0/max=1 (L171-177), perUserLimit=null isola o ramo order_id @unique.
  Invariante coupon-redeem-limits (facet idempotencia-por-pedido): exercitada no DB (indice UNIQUE order_id + net redeemed_count>=0/<=max). audit-same-tx NAO listada e correto: redeemCoupon corre na tx do checkout, nao grava audit_log (nao e mutacao admin auditada).
  Produto intocado: git diff e0e83a1..HEAD -- app lib prisma VAZIO. So infra de teste (_run-seam.ts case redeemCoupon ja existente/verde nas irmas, spec) mudou.
  Obs (nao reprova, QA nao edita spec): comentario L40-43 superestima que increment errado "estoura o CHECK" (CHECK e so >=0); irrelevante p/ o veredito pois A3 assere redeemed_count==1 direto via pg + net <=max explicito.

[QA] 2026-06-17 cupom.deactivate.audited -> APROVADO (passes=true, qa_note=null, state inalterado=ready_for_qa).
  Repro: pnpm harness tests/harness/pedido/cupom-deactivate-audited.spec.ts em PG efemero recem-seedado (28 prod/8 ped) -> 1 passed (5.4s).
  Anti-fake-green: 3/3 asserts provados ponta a ponta via pg sobre DATABASE_URL; seam=setCouponActive de PRODUCAO (lib/data/coupons.ts:213) via _run-seam.ts case setCouponActive (L476-484, sem mock); createCoupon (L150) semeia o cupom ATIVO. Source confere: setCouponActive corre 1 prisma.$transaction { findUnique(before) -> tx.coupon.update({data:{isActive}}) SO o campo isActive -> writeAuditLog(tx,...) action = isActive?coupon_update:coupon_deactivate }.
    A1 is_active==false sem apagar: pre-cond anti-trivial L150-156 assere is_active=TRUE no DB antes (flip nao-vacuo); pos-desligar L184-189 pg COUNT==1 (linha sobrevive, soft != delete) E is_active==false. Bonus L191-195: redeemed_count preservado==R (anti-trivial R=4>0 forcado direto no DB L160-161; se a funcao regravasse o cupom inteiro pegaria a regressao) + Number.isInteger.
    A2 1 linha coupon.deactivate na MESMA tx, before/after: L164-167 baseline GLOBAL A0; L199-205 delta GLOBAL ==A0+1 (exatamente 1 escrita de audit, nem 0 nem 2); L209-214 exatamente 1 linha action='coupon.deactivate' (DB dotted; enum @map prisma/schema.prisma:58) p/ esse entity_id UNICO do run; L215-216 before.isActive=true / after.isActive=false; L218-226 0 linhas coupon.update ainda (action depende do destino).
    A3 religar audita coupon.update: L231-234 baseline GLOBAL A1; pos-religar L246-250 is_active==true; L253-259 delta GLOBAL ==A1+1; L262-270 exatamente 1 linha action='coupon.update' (@map prisma/schema.prisma:57) p/ o entity_id com before.isActive=false/after.isActive=true; L277-282 a linha coupon.deactivate continua sendo a UNICA (religar nao duplicou). NAO e coupon.deactivate.
  Invariante audit-same-tx (unica listada): exercitada nas DUAS mutacoes via delta GLOBAL +1 medido (nao so presenca) + writeAuditLog dentro do prisma.$transaction de setCouponActive, action/before/after corretos, sem orfao. Nenhuma outra invariante listada e correto (setCouponActive nao toca preco/estoque).
  Produto intocado: git diff e0e83a1..HEAD -- app lib prisma VAZIO. Commit da feature (311458f) so tocou _run-seam.ts (INFRA), spec, ledger (state todo->ready_for_qa, sem mexer asserts/passes) e progress. Field ownership respeitado.

[QA] 2026-06-17 cupom.delete.unused-ok -> APROVADO (passes=true, qa_note=null, state inalterado=ready_for_qa).
  Repro: pnpm harness tests/harness/pedido/cupom-delete-unused-ok.spec.ts em PG efemero recem-seedado (28 prod/8 ped) -> 1 passed (4.5s).
  Anti-fake-green: 2/2 asserts provados ponta a ponta via pg sobre DATABASE_URL; seam=deleteCoupon de PRODUCAO (lib/data/coupons.ts:257) via _run-seam.ts case deleteCoupon (L499-507, sem mock); createCoupon (L150) p/ semear o cupom. Source confere: deleteCoupon corre 1 prisma.$transaction { findUnique(before) -> count(coupon_redemptions) (>0 => 'in_use' SEM apagar) -> tx.coupon.delete -> writeAuditLog(tx, action=coupon_delete, before=couponSnapshot, after=null) }.
    A1 ok:true + linha some de coupons (hard-delete): pre-cond anti-trivial L186-204 assere cupom EXISTE (COUNT==1) e ZERO redencoes (ramo unused-ok, nao vacuo); L206-210 forca redeemed_count=R=3>0 SEM inserir coupon_redemptions -> prova que a guarda olha o HISTORICO (count coupon_redemptions), nao o contador denorm (diferencia de cupom.delete.used); L221 chama deleteCoupon de PRODUCAO; L224-228 del.ok=true e del.id==couponId; L234-237 COUNT(*) em coupons p/ o id passa 1->0 via pg (hard-delete, nao soft).
    A2 1 linha coupon.delete na MESMA tx, before=snapshot, after=null: L213-216 baseline GLOBAL A0; L244-247 delta GLOBAL ==A0+1 (exatamente 1 escrita, nem 0 nem 2); L251-256 exatamente 1 linha action='coupon.delete' (DB dotted; enum @map prisma/schema.prisma:59) p/ esse entity_id UNICO do run; L258-264 before=snapshot do dominio batendo (code UPPER/type=percent/percentOff=15/valueCents=null/minSubtotalCents=5000/isActive=true, == couponSnapshot lib/data/coupons.ts:67); L267 after IS NULL; L270-278 0 linhas coupon.deactivate (anti-confusao: nao foi soft-delete disfarcado). audit-same-tx: writeAuditLog dentro do prisma.$transaction do delete (atomico, sem orfao).
  Invariantes coupon-delete-guard + audit-same-tx (as 2 listadas): exercitadas no DB. delete-guard: sanity de schema L151-170 assere FK coupon_redemptions.coupon_id confdeltype='r' (RESTRICT, confirmado prisma/schema.prisma:269) — senao a guarda seria vacua; delete procede SO porque count==0 e R>0 nao bloqueia. audit-same-tx: delta GLOBAL +1 medido + before/after corretos na mesma tx.
  Produto intocado: git diff e0e83a1..HEAD -- app lib prisma VAZIO. Commit da feature (dd2c3ab) so tocou _run-seam.ts (INFRA, +case deleteCoupon), spec, ledger (state->ready_for_qa) e progress. Field ownership respeitado.

[QA] 2026-06-17 cupom.delete.used-blocked -> APROVADO (passes=true, qa_note=null, state inalterado=ready_for_qa).
  Repro do ZERO: pnpm harness tests/harness/pedido/cupom-delete-used-blocked.spec.ts em PG efemero recem-seedado (db push + suplemento + seed 28 prod/8 ped) -> 1 passed (3.6s; runner 8.5s). Teardown "could not receive data from client" e cosmetico pos-pass.
  Anti-fake-green: 4/4 asserts provados ponta a ponta via pg sobre DATABASE_URL; seams de PRODUCAO via _run-seam.ts SEM mock: deleteCoupon (lib/data/coupons.ts:257, case L499-507), redeemCoupon (coupons.ts:364, case L508-525 envelopado em $transaction como o checkout), createCoupon (L150) p/ semear, setCouponActive (L213, case L490-498). O "ja usado" e criado pela FUNCAO DE PRODUCAO redeemCoupon (1 linha REAL em coupon_redemptions sobre pedido proprio inserido em pg), NAO uma linha falseada — setup mais forte que o irmao unused-ok.
    A1 ok:false + msg 'Cupom ja foi utilizado...': L255 chama deleteCoupon de PRODUCAO; L258 del.ok===false; L260-263 del.error.startsWith("Cupom já foi utilizado") (== COUPON_IN_USE coupons.ts:241-242, retornado no ramo redemptions>0 L264/L280). Anti-trivial: L237-244 assere EXATAMENTE 1 redencao real ANTES (ramo in_use alcancado, nao not_found/vacuo).
    A2 linha PERMANECE em coupons (FK Restrict): L266-274 COUNT(*) coupons p/ id ==1 pos-delete; L275-278 is_active ainda true (registro intocado); L281-293 historico de redencao sobrevive com discount_cents=1500 intacto. Via pg.
    A3 audit_log NAO ganha coupon.delete: L247-250 baseline GLOBAL A0; L296-302 delta GLOBAL ==0 (nenhuma escrita); L304-312 exatamente 0 linhas action='coupon.delete' (dotted cru) p/ entity_id UNICO do run. Via pg.
    A4 caminho correto = INATIVAR: L315-319 setCouponActive(...,false) de PRODUCAO; L320-324 ok; L326-338 COUNT==1 (nao apagou) e is_active==false; L341-348 historico preservado. Via pg.
  Invariantes (2/2 listadas): coupon-delete-guard exercitada — sanity de schema L177-196 assere FK coupon_redemptions.coupon_id confdeltype='r' (RESTRICT, confirmado prisma/schema.prisma:269; senao a guarda seria vacua); delete RECUSADO porque count(coupon_redemptions)>0; historico financeiro intacto. audit-same-tx exercitada — delete curto-circuita em "in_use" (coupons.ts:264) ANTES de writeAuditLog -> delta GLOBAL de audit==0 + 0 linhas coupon.delete (nenhum orfao na recusa).
  Produto intocado: git diff e0e83a1..HEAD -- app lib prisma VAZIO. Commit da feature (e08fbd2) so tocou admin_features.json (state todo->ready_for_qa, passes deixado false p/ QA), tests/harness/harness-progress.txt e o novo spec. Field ownership respeitado (asserts/invariants IMUTAVEIS; passes/qa_note escritos so agora pelo QA). VEREDITO: passes=true.
