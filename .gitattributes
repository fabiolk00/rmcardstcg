# Normaliza fim de linha para LF em todo o repo (gate de format:check
# deterministico entre Windows e o CI Linux).
* text=auto eol=lf

# Binarios
*.png binary
*.ico binary
*.woff binary
*.woff2 binary
